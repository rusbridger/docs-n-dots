# Docs 'N Dots

Some files and folders should be renamed with dots when loaded to the home directory.

- `config -> ~/.config`
- `gitconfig -> ~/.gitconfig`
- `ncmpcpp -> ~/.ncmpcpp`
- `scripts -> ~/.scripts`
- `xmonad -> ~/.xmonad`

